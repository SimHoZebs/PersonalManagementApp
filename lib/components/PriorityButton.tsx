import { Icon } from "@iconify/react";
import IconButton from "./IconButton";

const PriorityButton = () => {
  return (
    <IconButton>
      <Icon icon="ci:flag-fill" className="h-6 w-6" />
    </IconButton>
  );
};

export default PriorityButton;
